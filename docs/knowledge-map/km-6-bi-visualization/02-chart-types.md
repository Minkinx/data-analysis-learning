# 图表类型详解

> 每种图表类型都有其天然适合的**数据关系**和**分析目标**。选用错误图表不仅会降低信息传递效率，更可能导致读者得出错误结论。本节逐一讲解常用图表的适用场景和常见陷阱。

## 柱状图（Bar Chart）

**适用场景**：类别间数值比较、排名、时间序列（离散时间点）。

```text
✅ 正确用法
- 横向条形图（类别名较长时）
- 按数值排序（排名场景）
- 柱状图 + 参考线（对比目标）

❌ 常见错误
- 起始轴不从 0 开始 → 放大差异，误导读者
- 柱状图堆叠超过 3 层 → 中间层的对比变得困难
- 使用 3D 效果 → 遮挡视角，引入透视变形
```

::: warning 轴必须从 0 开始
柱状图编码在**长度**上，如果 y 轴不从 0 开始，柱子的长度比例会被扭曲。这是可视化中**最常被滥用**的误导手法。折线图和散点图可以从非零起点开始，但柱状图不行。
:::

## 折线图（Line Chart）

**适用场景**：连续时间序列趋势、多个序列对比、预测与实际对比。

```text
✅ 正确用法
- 时间在 x 轴，按时间顺序排列
- 多条线时使用不同颜色+线型
- 添加置信区间/阴影带

❌ 常见错误
- 连接不连续的数据点（不要让折线"跨空"）
- 超过 5 条线（变成"意面图" Spaghetti Chart）
- 过分平滑数据（引入误解，掩盖真实波动）
```

**小样本建议**：数据点少于 10 个时，在每个数据点标注具体数值；多于 50 个点时，可以考虑减少标注，突出总体趋势即可。

## 散点图（Scatter Plot）

**适用场景**：两个连续变量的关系、相关性分析、异常值检测、聚类展示。

```text
✅ 正确用法
- 添加趋势线 + 置信区间
- 用颜色编码第三维度
- 对大样本数据做透明度处理（Alpha Blending）

❌ 常见错误
- 数据点过多且无透明度 → 完全重叠，看不出密度
- 过度解读相关性 → 相关性 ≠ 因果性
- 忘记标注坐标轴单位
```

## 直方图（Histogram）

**适用场景**：观察单变量数值分布、检查正态性、发现数据偏态或双峰。

```text
✅ 正确用法
- 根据数据量和范围选择合适的 bin 数量
- 叠加正态分布曲线辅助判断

❌ 常见错误
- bin 宽度过小 → 噪声过多，看不到整体形态
- bin 宽度过大 → 丢失分布细节
- 用柱状图的间隙 → 直方图的柱之间不应有间隙
```

**Bin 数量经验公式**：对于 n 个数据点，推荐 bin 数 ≈ `2 * n^(1/3)`（Sturges 规则）或 `3.5 * σ * n^(-1/3)`（Scott 规则）。

## 箱线图（Box Plot）

**适用场景**：多组数据分布对比、异常值检测、识别偏态。

箱线图的五个统计量：最小值（不含异常值）、Q1（第一四分位数）、中位数、Q3（第三四分位数）、最大值（不含异常值）。

```text
✅ 正确用法
- 多组箱线图并列展示，适合对比
- 结合散点图展示实际数据点（Beeswarm 叠加）

❌ 常见错误
- 样本量太小时使用（< 10 个点，箱线图不稳定）
- 只展示箱线图而不展示样本量 n
- 误解须（Whisker）含义：须 ≠ 最大最小值（受 IQR 约束）
```

**须的计算**：通常 `min(Q1 - 1.5*IQR, 实际最小值)` 到 `max(Q3 + 1.5*IQR, 实际最大值)`，超出此范围的点标记为异常值。

## 热力图（Heatmap）

**适用场景**：二维矩阵数值展示、相关性矩阵、时间序列的日历视图（Calendar Heatmap）、地理热力。

```text
✅ 正确用法
- 使用顺序色或发散色，颜色深浅对应数值
- 对行/列进行聚类（Clustered Heatmap）

❌ 常见错误
- 颜色跨度不连续 → 误导读者对数值差异的判断
- 行列过多且不聚类 → 无法发现模式
```

## 树图（Treemap）

**适用场景**：展示层级占比、有限空间内的多级类别展示、文件/资源占用分析。

```text
✅ 正确用法
- 面积代表数值大小，颜色代表类别或二级指标
- 配合悬停显示详细数值

❌ 常见错误
- 层级超过 3 层 → 视觉上难以分辨
- 数据项超过 30 个 → 小方块难以看清
- 不使用饼图的替代 → Treemap 是很多饼图的替代方案，但并非万能
```

## 瀑布图（Waterfall Chart）

**适用场景**：构成因素的增减分解（利润拆解、现金流量变化）、从初始值到最终值的变化路径。

```text
✅ 正确用法
- 起始和结束用全高柱，中间用浮动柱
- 增加/减少用不同颜色区分

❌ 常见错误
- 堆积项过多 → 图表变得杂乱
- 不标注具体数值 → 读者需要自行估算变化量
```

## 其他常用图表

| 图表类型 | 适用场景 | 注意事项 |
|---------|---------|---------|
| **面积图（Area Chart）** | 强调累积量趋势 | 堆叠面积图超过 3 层后底部趋势难以分辨 |
| **雷达图（Radar Chart）** | 多维度评分对比 | 不超过 6 个维度，否则"蜘蛛网"失去可读性 |
| **气泡图（Bubble Chart）** | 三维数据展示（x, y, 气泡大小） | 面积感知有偏差，数据点避免重叠 |
| **桑基图（Sankey Diagram）** | 流量/转化/路径分析 | 节点不宜过多，流量宽度按比例展示 |
| **漏斗图（Funnel Chart）** | 转化率分析 | 注意：漏斗每层的量纲应该一致 |
| **小提琴图（Violin Plot）** | 分布+密度展示 | 样本量较小时不稳定，需要 > 50 个点 |

## 图表选择的黄金法则

1. **最小化读者的认知负担** — 最常见的图表类型（柱状图、折线图、散点图）通常是最好的选择
2. **一个图表传递一个信息** — 不要试图在一个图表中塞入过多维度
3. **先写结论，再选图表** — 明确你想表达的核心观点，然后选择最能支持该观点的图表
4. **测试你的图表** — 让不了解数据的人看 5 秒，问他们看到了什么

## 相关文章

- [可视化原理](/knowledge-map/km-6-bi-visualization/01-principles) — Gestalt 原则、预注意属性、色彩理论
- [Dashboard 设计](/knowledge-map/km-6-bi-visualization/03-dashboard-design) — 如何将图表组合成有效仪表板
